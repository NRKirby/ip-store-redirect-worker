{
  "private": true,
  "name": "ip_store_redirect_worker",
  "version": "1.0.0",
  "description": "A simple Cloudflare worker that stores client IP addresses in a KV store before redirecting to a URL.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Nick Kirby <nrkirb@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
